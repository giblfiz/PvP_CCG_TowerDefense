# PvP CCG Tower Defense

## Setup

To ensure you're using the exact same package versions:

```bash
# Run the setup script
./setup.sh
```

## Available Commands

- `npm run test` - Run Jest tests
- `npm run lint` - Run ESLint